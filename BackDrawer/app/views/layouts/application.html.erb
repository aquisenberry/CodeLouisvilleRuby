<!DOCTYPE html>
<html>
<head>
  <title>BackDrawer</title>
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">

  <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.0.0-alpha1/jquery.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/js/bootstrap.min.js"></script>

  
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<div class="container-fluid header">
  <div class="wrapper">
    <h1><%= link_to "Back Drawer", root_path %></h1>
    <ul class="nav nav-pills pull-right">
      <%if session[:user_id]%>
      <li><%= link_to User.find_by(id: session[:user_id]).username, user_path(session[:user_id]) %></li>
      <li><%= link_to "Resources", writer_resources_path %></li>
      <li><%= link_to "Log out", user_session_path(request.session_options[:id]), :method => :delete %></li> 
      <%else%>
      <li><%= link_to "Log In", new_user_session_path %></li>
      <li><%= link_to "Sign Up", new_user_path %></li>
      <%end%>
    </ul>
    <br class="clear" />
  </div>
</div>
 
<% flash.each do |type, message| %>
<div class="container">
	<div class="alert flash <%= type %>">
    <%= message %>
  </div>
</div>
  
<% end %>
<%= yield %>

</body>
</html>
